import Link from "next/link";import { Card, Hero, Section } from "@/components/ui";import { transmissions } from "@/lib/data";export default function Transmissions(){return <><Hero title="Коробки передач" subtitle="Центральний шар знань ShiftTech: кожна коробка має власний стабільний URL і зв'язки з авто, симптомами та майбутніми кейсами."/><Section eyebrow="entities" title="Перший список трансмісій"><div className="grid gap-5 md:grid-cols-3">{transmissions.map(t=><Card key={t.id} title={t.name}><p>{t.alt}</p><p className="mt-2 text-sm text-zinc-500">Тип: {t.type}</p><Link className="mt-4 inline-block text-shift" href={`/transmissions/${t.id}`}>Відкрити сторінку</Link></Card>)}</div></Section></>}
