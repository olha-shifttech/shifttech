import Link from "next/link";
import { Card, Hero, Section } from "@/components/ui";
import { serviceCards, symptoms, transmissions, vehicles, vehicleName } from "@/lib/data";

export default function Home() {
  return <><Hero title="Преміальна платформа ShiftTech для ремонту АКПП, DSG і CVT" subtitle="Сайт побудований як AI-friendly Knowledge Platform: клієнт може почати з автомобіля, симптому або коробки, а система зберігає центр знань у трансмісії." /><Section eyebrow="architecture" title="Dual Navigation: людина шукає авто, система пов'язує коробку"><div className="grid gap-5 md:grid-cols-3"><Card title="Вхід через автомобіль" href="/cars"><p>Клієнт часто знає марку, модель, двигун або симптом, але не код коробки.</p></Card><Card title="Центр — трансмісія" href="/transmissions"><p>09G, 09M, 09K, Jatco CVT та DSG стають вузлами майбутньої бази знань.</p></Card><Card title="AI-ready структура" href="/ai-search"><p>Сторінки розділені на сутності, щоб їх можна було масштабувати до AI-пошуку, API та інтеграцій.</p></Card></div></Section><Section eyebrow="services" title="Основні напрями першої версії"><div className="grid gap-5 md:grid-cols-2 lg:grid-cols-4">{serviceCards.map((card) => <Card key={card.title} title={card.title} href={card.href}><p>{card.text}</p></Card>)}</div></Section><Section eyebrow="knowledge core" title="Перший каркас даних"><div className="grid gap-5 lg:grid-cols-3"><Card title="Коробки передач"><ul className="space-y-2">{transmissions.slice(0, 8).map((item) => <li key={item.id}><Link className="text-shift" href={`/transmissions/${item.id}`}>{item.name}</Link> <span className="text-zinc-500">{item.alt}</span></li>)}</ul></Card><Card title="Автомобілі"><ul className="space-y-2">{vehicles.slice(0, 6).map((item) => <li key={item.slug}><Link className="text-shift" href={`/cars/${item.slug}`}>{vehicleName(item)}</Link></li>)}</ul></Card><Card title="Симптоми"><ul className="space-y-2">{symptoms.slice(0, 7).map((symptom) => <li key={symptom}>{symptom}</li>)}</ul></Card></div></Section></>;
}
