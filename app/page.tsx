import Link from "next/link";
import { Card, Hero, Section } from "@/components/ui";
import { serviceCards, symptoms, transmissions, vehicles } from "@/lib/data";
export default function Home(){return <><Hero title="Преміальна технічна платформа ShiftTech для АКПП, DSG і CVT" subtitle="Перша робоча версія сайту побудована навколо знань: клієнт може почати з автомобіля, симптому або типу коробки, а система зберігає центр даних у трансмісії."/><Section eyebrow="architecture" title="Dual Navigation: людина шукає авто, система пов'язує коробку"><div className="grid gap-5 md:grid-cols-3"><Card title="Вхід через автомобіль"><p>Клієнт часто знає марку, модель, двигун або симптом, але не код коробки.</p></Card><Card title="Центр — трансмісія"><p>09G, 09M, 09K, Jatco CVT та DSG стають вузлами майбутньої бази знань.</p></Card><Card title="AI-ready структура"><p>Сторінки розділені на сутності, щоб їх можна було масштабувати до AI-пошуку, API та інтеграцій.</p></Card></div></Section><Section eyebrow="services" title="Основні напрями першої версії"><div className="grid gap-5 md:grid-cols-2 lg:grid-cols-4">{serviceCards.map(c=><Card key={c.title} title={c.title}><p>{c.text}</p></Card>)}</div></Section><Section eyebrow="knowledge core" title="Перший каркас даних"><div className="grid gap-5 lg:grid-cols-3"><Card title="Коробки передач"><ul className="space-y-2">{transmissions.slice(0,8).map(t=><li key={t.id}><Link className="text-shift" href={`/transmissions/${t.id}`}>{t.name}</Link> <span className="text-zinc-500">{t.alt}</span></li>)}</ul></Card><Card title="Автомобілі"><ul className="space-y-2">{vehicles.slice(0,6).map(v=><li key={v.name}>{v.name}</li>)}</ul></Card><Card title="Симптоми"><ul className="space-y-2">{symptoms.slice(0,7).map(s=><li key={s}>{s}</li>)}</ul></Card></div></Section></>}
