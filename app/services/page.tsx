import { Card, Hero, Section } from "@/components/ui";import { serviceCards } from "@/lib/data";export default function Services(){return <><Hero title="Послуги ShiftTech" subtitle="Сторінка фіксує напрями роботи без вигаданих цін, адрес або гарантійних умов. Точні рішення — тільки після діагностики."/><Section eyebrow="service map" title="Напрями сервісу"><div className="grid gap-5 md:grid-cols-2">{serviceCards.map(c=><Card key={c.title} title={c.title}>{c.text}</Card>)}</div></Section></>}
